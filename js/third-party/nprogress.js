"use strict";NProgress.configure({showSpinner:CONFIG.nprogress.spinner}),NProgress.start(),document.addEventListener("readystatechange",function(){"interactive"===document.readyState&&NProgress.inc(.8),"complete"===document.readyState&&NProgress.done()}),document.addEventListener("pjax:send",function(){NProgress.start()}),document.addEventListener("pjax:success",function(){NProgress.done()});